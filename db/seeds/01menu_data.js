
exports.seed = function(knex, Promise) {
  // Deletes ALL existing entries
  // return knex('menu').del()
  //   .then(function () {
  //     return Promise.all([
  //       // Inserts seed entries
  //       knex('menu').insert({id: 1, name: 'pizza'})
  //     ]);
  //   });
};

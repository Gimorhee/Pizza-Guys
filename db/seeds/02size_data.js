
exports.seed = function(knex, Promise) {
  // Deletes ALL existing entries
  // return knex('sizes').del()
  //   .then(function () {
  //     return Promise.all([
  //       // Inserts seed entries
  //       knex('sizes').insert({id: 1, type: 'small', price: 10}),
  //       knex('sizes').insert({id: 2, type: 'medium', price: 20}),
  //       knex('sizes').insert({id: 3, type: 'large', price: 30})
  //     ]);
  //   });
};
